// Search for functions that hook into a filter or action
// Leave off closing mark
// Search with and without spaces between ( and the '

add_filter( 'wp_title // OR  without spaces add_filter('wp_title
add_action( 'init

// See where filter or hook is applied

apply_filters( 'wp_title
do_action( 'init